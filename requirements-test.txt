# Test dependencies for Advanced Shop Finder

# Main dependencies (same as production)
aiohttp>=3.8.0
tqdm>=4.65.0
backoff>=2.2.0

# Testing dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0
